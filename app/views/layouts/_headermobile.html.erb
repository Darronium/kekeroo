<header class="navbar">
<div class = "container">
<ul class="nav navbar-nav navbar-right">

<li class="dropdown">
	<a href="users" class="dropdown-toggle" data-toggle="dropdown" style="border-bottom: solid;">
	 Navigate <b class="caret"></b>
	</a>
	<ul class="dropdown-menu">



					<header class="navbar">
						<div class = "container">
							<% if logged_in? %>
								<ul class="nav navbar-nav", style = "color: <%= current_user.color_code %>; border-bottom: 0.1px solid #fdfdf5;" >
									<li>
										<div class = "avatar-small-<%= current_user.avatar %> currentuser" style = "display: inline-block;"></div>
										<%= link_to current_user.username, current_user, class: "currentusername", style: "display: inline-block;" %>
									</li>
								</ul>
							<% end %>
							<ul class="nav navbar-nav">
								<% if logged_in? %>
						          <li><%= link_to "Users", "/users" %></b></li>

						           <ul style = "padding-bottom: 10px;">
						              <% User.all.each do |user| %>
						              	<% if user.status %>
										    <li class = "userdropdown">
										    	<%= link_to user, class: "userlist" do %>
										    		<div class = "onlinelist avatar-small-<%= user.avatar %>"></div>
										    		<span class = "dropdownname" style = "color: <%= user.color_code %>;"><%= user.username %> </span>
										    	<% end %>
										    </li>
										<% end %>
									  <% end %>
						            </ul>
						          
						        <% end %>
							    <li class = "navborder"><%= link_to "Home",   root_path %></li>
							    <li class = "navborder"><%= link_to "Download",   download_path %></li>
							    <li class = "navborder"><%= link_to "Help",   help_path %></li>
							    <li class = "navborder">
							    	<% if logged_in? %>
							            <%= link_to "Log out", logout_path, method: "delete" %>
							    	<% else %>
							    		<%= link_to "Sign Up",   signup_path %>	
							    	<% end %>
							    </li>
							</ul>
						</div>
					</header>

	</ul>
</li>
</ul>
</div>
</header>